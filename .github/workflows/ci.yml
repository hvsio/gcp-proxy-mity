name: Deploy Infrastructure and App to GCP

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: ${{ vars.REGION || 'europe-west4' }}
  SERVICE_NAME: gcp-proxy-mity

jobs:
  deploy:
    permissions:
      contents: 'read'
      id-token: 'write'

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Google Auth
        id: auth
        uses: 'google-github-actions/auth@v3'
        with:
          workload_identity_provider: '${{ secrets.WIF_PROVIDER }}'
          service_account: '${{ secrets.WIF_SERVICE_ACCOUNT }}'

      - name: Setup Google Cloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Create Artifact Registry repo (if not exists)
        run: |
          gcloud artifacts repositories describe $SERVICE_NAME \
            --location=$GCP_REGION --project=$PROJECT_ID 2>/dev/null || \
          gcloud artifacts repositories create $SERVICE_NAME \
            --repository-format=docker \
            --location=$GCP_REGION \
            --project=$PROJECT_ID

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker $GCP_REGION-docker.pkg.dev

      - name: Build Docker image
        run: |
          docker build -t $GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:$GITHUB_SHA .
          docker tag $GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:$GITHUB_SHA \
            $GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:latest

      - name: Push Docker image
        run: |
          docker push $GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:$GITHUB_SHA
          docker push $GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:latest

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Create terraform.tfvars
        run: |
          cd terraform
          cat > terraform.tfvars << EOF
          project_id              = "${{ secrets.GCP_PROJECT_ID }}"
          region                  = "$GCP_REGION"
          backend_container_image = "$GCP_REGION-docker.pkg.dev/$PROJECT_ID/$SERVICE_NAME/$SERVICE_NAME:$GITHUB_SHA"
          allowed_iap_user_email  = "${{ vars.ALLOWED_IAP_USER_EMAIL }}"
          frontend_domain         = "${{ vars.FRONTEND_DOMAIN }}"
          EOF

      - name: Terraform Init
        run: |
          cd terraform
          terraform init

      - name: Terraform Plan
        run: |
          cd terraform
          terraform plan -out=tfplan

      - name: Terraform Apply
        run: |
          cd terraform
          terraform apply -auto-approve tfplan

      - name: Get Deployment Info
        run: |
          cd terraform
          echo "## Deployment Complete! 🚀" >> $GITHUB_STEP_SUMMARY
          echo "- **IAP-protected URL:** $(terraform output -raw iap_protected_url)" >> $GITHUB_STEP_SUMMARY
          echo "- **Load Balancer IP (for DNS A record):** $(terraform output -raw load_balancer_ip)" >> $GITHUB_STEP_SUMMARY
          echo "- **Backend (internal):** \`terraform output -raw backend_cloud_run_uri\`" >> $GITHUB_STEP_SUMMARY
